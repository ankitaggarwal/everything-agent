# Copy to .env and fill in. Never commit your real .env.

# Tier 2 Agent brain (Claude Agent SDK) and Tier 1 reflex when provider=anthropic
ANTHROPIC_API_KEY=

# Tier 1 reflex when provider=gemini
GEMINI_API_KEY=

# Cartesia TTS provider (only needed if providers.cartesia.enabled = true)
CARTESIA_API_KEY=
