# Core (laptop + robot). Audio I/O is sounddevice on both; the robot also has the
# reachy_mini SDK (preinstalled in its venv) for head/antenna motion.
google-genai>=2.8.0
numpy>=1.26
PyYAML>=6
sounddevice>=0.4.6
